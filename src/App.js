import styled from "styled-components";

const Title = styled.h1`
  color: orange;
`;

const App = () => {
  return (
    <div>
      <Title>Progress Tracker</Title>
    </div>
  );
};

export default App;
